# 构建产物
.gradle/
build/
app/build/
dist/
out/
local.properties

# IDE
.idea/
*.iml

# 临时
*.log
.DS_Store

# 本地构建产物（通过 CI artifact 分发，不入库）
app-debug-v*.apk
crash_last.txt

# Python 字节码（py_compile / import 产生）
__pycache__/

# 本地工具链（kotlinc / JDK / android.jar，供 scripts/kt_check.sh 与
# tools/run_sampling_tests.sh 离线使用）。体积上百 MB，按需下载，不入库。
.omnibot/

# qemu-user 崩溃时产生的 core（ABI 验证用，不入库）
*.core

# Gradle/CMake 本地构建中间产物
.cxx/
